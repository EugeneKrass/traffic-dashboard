rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /trafficStats/{document} {
      allow read: if request.auth != null;
      allow write: if false; // Only by Cloud Functions
    }
  }
}